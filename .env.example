# Copy to .env and edit for your device.
# Find the vendor:product ID with: lsusb
DEVICE_ID=5131:2019

# The key the button emits out-of-the-box.
# Find it with: sudo evtest /dev/input/by-id/usb-<vendor>_<product>-event-kbd
# keyd uses short names: 2, a, kp1, ... (see keyd(5))
SOURCE_KEY=2
